<%= link_to gravatar_for(current_user, size: Settings.gravatar_preferred_size), current_user %>
<h1><%= current_user.name %></h1>
<span><%= link_to t("static_pages.home.view_profile_link"), current_user %></span>
<span><%= pluralize current_user.microposts.size, t("static_pages.home.txt_mircopost") %></span>
